# dependencies
node_modules/

# runtime state (used-question history) — lives on the server, not in git
data/

# environment / secrets
.env
*.env
*.pem

# OS / editor noise
.DS_Store
*.log
npm-debug.log*
